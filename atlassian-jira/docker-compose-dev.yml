jira:
  extends:
    file: jira.yml
    service: jira
  links:
    - database
  volumes:
    - home:/opt/jira-home

database:
  image: postgres:9.4
  volumes:
    - setup-jira-db.sh:/docker-entrypoint-initdb.d/setup-jira-db.sh
